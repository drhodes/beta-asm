module Uasm.Pretty where

import Text.PrettyPrint.Leijen
import Uasm.Parser 


-- NOPE need to parse comments and that's not going to happen yet.

-- instance Pretty Ident where
--   pretty (Ident s) = text s
--   pretty (C

          
-- instance Pretty Binop where
--   pretty x = text $ case x of
--     Multiply -> "*" 
--     Subtract -> "-" 
--     Addition -> "+" 
--     Division -> "/" 
--     RightShift -> ">>" 
--     LeftShift -> "<<" 
--     Modulo -> "%" 
--     BitwiseComplement -> "~" 
--     BitwiseAnd -> "&" 
--     BitwiseOr -> "|" 


